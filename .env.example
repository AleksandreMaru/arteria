# Copy values into Cloudflare Pages → Settings → Environment variables
# (Production + Preview). Do not commit real secrets.

RESEND_API_KEY=re_xxxxxxxx
REGISTER_TO_EMAIL=you@example.com
# Optional once you verify a domain in Resend:
# REGISTER_FROM_EMAIL=ARTAREA ED <hello@yourdomain.com>
